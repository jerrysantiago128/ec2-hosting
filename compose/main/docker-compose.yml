version: '3.8'

services:
  rovaly-web-app-main:
    image: rovaly-web-app:sprint-1-main
    container_name: rovaly-web-app-main
    ports:
      - "8080:8080"
    expose:
      - 8080
    networks:
      - rovaly-suite

networks:
  rovaly-suite:
    external: true