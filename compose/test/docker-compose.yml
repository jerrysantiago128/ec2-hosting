version: '3.8'

services:
  rovaly-web-app:
    image: rovaly-web-app:sprint-1-test
    container_name: rovaly-web-app-test
    ports:
      - "8081:8081"
    expose:
      - 8080
    networks:
      - rovaly-suite

networks:
  rovaly-suite:
    external: true