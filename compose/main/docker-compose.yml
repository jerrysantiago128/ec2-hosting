version: '3.8'

services:
  rovaly-web-app:
    image: rovaly-web-app:spirnt-1-main
    container_name: rovaly-web-app-main
    ports:
      - "8080:8080"
    expose:
      - 8080
    networks:
      - rovaly-suite

networks:
  rovaly-suite:
    external: true